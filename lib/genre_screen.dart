import 'package:flutter/material.dart';

class GenreScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Genres')),
      body: Center(child: Text('This is the genre screen.')),
    );
  }
}
